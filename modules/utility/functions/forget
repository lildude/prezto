# Forgetting everything about a known host
# Host as given
ssh-keygen -R $1
# IP address based entries
ssh-keygen -R `grep $1 /etc/hosts | awk '{ print $1 }'`
# FQHN entries
ssh-keygen -R `grep $1 /etc/hosts | awk '{ print $2 }'`
